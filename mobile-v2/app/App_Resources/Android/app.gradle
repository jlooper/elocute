// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.ladeezfirstmedia.elocute"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
